server {
  listen   8080;

  root /default;

  location / echo {
  }

  location /images/ static {
    root /static_files;
  }

  location /text/ static {
    root /static_files;
  }

  location /applications/ static {
    root /static_files;
  }

}