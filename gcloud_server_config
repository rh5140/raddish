server {
  listen   80;

  root /;

  location / echo {
  }

  location /echo echo {
  }

  location /images/ static {
    root /static_files;
  }

  location /text/ static {
    root /static_files;
  }

  location /applications/ static {
    root /static_files;
  }
}
